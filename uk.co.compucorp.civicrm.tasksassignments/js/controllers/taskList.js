define(['controllers/controllers'], function(controllers){    controllers.controller('TaskListCtrl',['$scope', '$modal', '$rootElement', '$log', 'taskList', 'settings',        function($scope, $modal, $rootElement, $log, taskList, settings){            $log.debug('Controller: TaskListCtrl');            $scope.taskList = taskList;            $scope.open = function() {                var modalInstance = $modal.open({                    targetDomEl: $rootElement.find('div').eq(0),                    templateUrl: settings.pathTpl+'modal/task.html?v='+(new Date().getTime()),                    controller: 'ModalTaskCtrl'                });                modalInstance.result.then(function(results){                    $log.info(results);                }, function(){                    $log.info('Modal dismissed');                });            }        }]);});