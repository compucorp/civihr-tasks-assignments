define(['directives/directives'], function(directives){    directives.directive('civiEditable',['$rootScope','$compile','$log', function($rootScope, $compile, $log){        $log.debug('Directive: civiEditable');        return {            priority: 0,            compile: function(tEl, tAttr) {                tEl.append('<i class="fa fa-pencil" />');            }        };/*        return {            priority: 1000,            replace: false,            terminal: true,            compile: function(tEl, tAttr) {                console.log('compile civiEditable');                tEl.wrap('<span class="" />');                tEl.after('<span />');                tEl.removeAttr('civi-editable');                return {                    pre: function preLink(scope, iElement, iAttrs, controller) { },                    post: function postLink(scope, iElement, iAttrs, controller) {                        $compile(iElement)(scope);                    }                };            }        }        */    }]);});